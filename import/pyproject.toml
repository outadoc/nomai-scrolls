[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "nomai-import"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
nomai-import = "nomai_import.__main__:main"

[tool.hatch.build.targets.wheel]
packages = ["src/nomai_import"]
